name: semgrep
domain: code
capabilities:
  - static-analysis
  - source-scanning
runtime:
  image: redforge/code-runtime
  entrypoint: semgrep
  version: 1.95.0
inputs:
  target: source-dir
output:
  format: json
trust:
  verified: false
  source: semgrep/semgrep (official release)
