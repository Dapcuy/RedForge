name: ffuf
domain: web
capabilities:
  - fuzzing
  - content-discovery
runtime:
  image: redforge/web-runtime
  entrypoint: ffuf
  version: 2.1.0
inputs:
  target: url
output:
  format: json
trust:
  verified: false
  source: ffuf/ffuf (official release)
